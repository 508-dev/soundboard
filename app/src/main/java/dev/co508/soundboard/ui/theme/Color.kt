package dev.co508.soundboard.ui.theme

import androidx.compose.ui.graphics.Color

// Fallback palette for devices without dynamic color (below Android 12).
// Teal reads as "audio" without competing with the sibling app's blue.
val Teal40 = Color(0xFF00696E)
val Teal80 = Color(0xFF4FD8E0)
val Amber40 = Color(0xFF7A5900)
val Amber80 = Color(0xFFEEC148)
val Neutral10 = Color(0xFF141218)
val Neutral99 = Color(0xFFFFFBFE)
