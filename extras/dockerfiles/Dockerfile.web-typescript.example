FROM oven/bun:1.3.13 AS build

WORKDIR /app

COPY package.json bun.lock bunfig.toml biome.json ./
COPY stacks/typescript stacks/typescript

RUN bun install --frozen-lockfile
RUN bun run --cwd stacks/typescript build
